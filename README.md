# Sixtyfour Workflow Engine Replica

## How to Start

1. pull code
2. cd into frontend, then run "npm run dev" click on localhost link
3. cd .., then cd backend, the run "uvicorn endpoints:app --reload"

If I can get this deployed before the call, then hopefully I can have a link!
